# bordinfinity-node-test
route /add
with add route we can add data(post route)
Schema
- task name
- task description
- creator
- duration
- createdAt

route /list
with list route we can see list (get route)

After the duration data will deleted

api link <a href="https://bordinfinity-node.herokuapp.com/">Preview</a>
